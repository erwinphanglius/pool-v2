{
  "name": "pool-v2",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ erwinphanglius/pool-v2",
    "create-local": "graph create --node http://localhost:8020/ erwinphanglius/pool-v2",
    "remove-local": "graph remove --node http://localhost:8020/ erwinphanglius/pool-v2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 erwinphanglius/pool-v2"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.1",
    "@graphprotocol/graph-ts": "0.23.1"
  }
}
