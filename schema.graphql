type Factory @entity {
  id: ID!
  timestamp: BigInt!
  poolAddress: Bytes! # address
}

type User @entity {
  id: ID!
  pool: [PoolByUser!]!
}

type PoolByUser @entity {
  id: ID!
  user: Bytes! # address
  value: BigInt!
}