type Factory @entity {
  id: ID!
  timestamp: BigInt! # address
  poolAddress: Bytes! # address
}

type Pool @entity {
  id: ID!
}

type PoolParticipant @entity {
  id: ID!
  user: Bytes!
  balance: BigInt!
  pool: Pool!
}